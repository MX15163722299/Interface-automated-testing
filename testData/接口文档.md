# 📘 接口文档（基于 Flask 项目自动生成）

 `flask项目.py` 全部接口说明，共 7 个接口：

## 用户注册
- **接口路径：** `/register`
- **请求方法：** `POST`
- **参数类型：** JSON
- **请求参数：** `username`, `password`, `age`, `sex`
- **是否依赖登录：** 否
- **返回示例：**
```json
{
  "uid": "u003",
  "username": "testuser",
  "errorcode": 0,
  "info": { "age": 20, "sex": "male" },
  "books": []
}
```

## 用户登录
- **接口路径：** `/login`
- **请求方法：** `POST`
- **参数类型：** 表单
- **请求参数：** `username`, `password`
- **是否依赖登录：** 否
- **返回示例：**
```json
{
  "uid": "u001",
  "username": "user1",
  "errorcode": 0,
  "info": {...},
  "books": [...]
}
```

## 获取用户信息
- **接口路径：** `/profile`
- **请求方法：** `GET`
- **参数类型：** Cookie
- **请求参数：** Cookie 中的 `uid`
- **是否依赖登录：** 是
- **返回示例：**
```json
{
  "uid": "u001",
  "username": "user1",
  "errorcode": 0,
  "info": { "age": 12, "sex": "male" },
  "books": [...]
}
```

## 查询商品信息
- **接口路径：** `/get_product`
- **请求方法：** `GET`
- **参数类型：** Query
- **请求参数：** `productid`
- **是否依赖登录：** 否
- **返回示例：**
```json
{
  "productid": "product1",
  "name": "Product One",
  "price": 10.99,
  "errorcode": 0
}
```

## 提交订单
- **接口路径：** `/submit_order`
- **请求方法：** `POST`
- **参数类型：** JSON + Cookie
- **请求参数：** `productid` + Cookie中的`uid`
- **是否依赖登录：** 是
- **返回示例：**
```json
{
  "message": "Order submitted successfully",
  "order_id": "d001"
}
```

## 查询订单
- **接口路径：** `/get_orders`
- **请求方法：** `GET`
- **参数类型：** Query + Cookie
- **请求参数：** `order_id`（可选）+ Cookie中的`uid`
- **是否依赖登录：** 是
- **返回示例：**
```json
{
  "orders": [...]
}
```

## 更新用户信息
- **接口路径：** `/update_user_info`
- **请求方法：** `POST`
- **参数类型：** JSON + Cookie
- **请求参数：** `username`, `info` + Cookie中的`uid`
- **是否依赖登录：** 是
- **返回示例：**
```json
{
  "message": "User information updated successfully",
  "errorcode": 0
}
```
